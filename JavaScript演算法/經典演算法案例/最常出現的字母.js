const my_max = str => {
    const words = {};

    for(let ch of str)
        words[ch] = words[ch] + 1 || 1;
    let counter = 0;
    let most = null;

    for(let ch in words){
        if(word[ch] || counter){
            if(words[ch] > words[ch]){
                countre = word[ch]
            }
        }
    };
    return mostD;9
}